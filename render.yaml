services:
  - type: web
    name: telegram-member-bot
    env: docker
    plan: free
    region: oregon
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ALLOWED_GROUP_ID
        sync: false
    healthCheckPath: /
    autoDeploy: true
